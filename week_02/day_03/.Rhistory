element_check(1,2)
elemnt_check(1,2)
elemnt_check(1,c(2,3))
elemnt_check(c(3,4),c(2,3))
elemnt_check(c(2,2,2),c(2,2,2))
elemnt_check(c(2,2,2),c(2,2,2))
elemnt_check(c(2,2,2),c(2,2,1))
elemnt_check(c(1,2,3),c(3,2,1))
elemnt_check(c(1,2,3),c(3,2,1))
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(sort(x)-sort(y))==0))
{
if (sum(abs(x-y))==0)
{
print("The same")
} else {
print("Exactly the same")}
} else {
print("Different Elements")}
} else {
print ("Different Length")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check(c(3,2,1),c(1,2,3))
elemnt_check(c(1,1,1),c(1,1,1))
elemnt_check(c(1,1,1),c(1,1,1))
elemnt_check(c(3,2,1),c(1,2,3))
elemnt_check(c(3,2,1),c(1,2,3))
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(sort(x)-sort(y))==0))
{
if (sum(abs(x-y))==0)
{
print("The same")
} else {
print("Exactly the same")}
} else {
print("Different Elements")}
#     } else {
#    print ("Different Length")}
#} else {
# print("Error: x and y must be vectors")}
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(sort(x)-sort(y))==0))
{
if (sum(abs(x-y))==0)
{
print("The same")
} else {
print("Exactly the same")}
} else {
print("Different Elements")}
#     } else {
#    print ("Different Length")}
#} else {
# print("Error: x and y must be vectors")}
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(sort(x)-sort(y))==0))
{
if (sum(abs(x-y))==0)
{
print("The same")
} else {
print("Exactly the same")}
} else {
print("Different Elements")}
#     } else {
#    print ("Different Length")}
#} else {
# print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(sort(x)-sort(y))==0))
{
if (sum(abs(x-y))==0)
{
print("The same")
} else {
print("Exactly the same")}
} else {
print("Different Elements")}
#     } else {
#    print ("Different Length")}
#} else {
# print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{
print("The same")
} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("Exactly the same")}
} else {
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{
print("The same")
} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("Exactly the same")}
#     } else {
#    print ("Different")}
#} else {
# print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{
print("The same")
} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("Exactly the same")} }
#     } else {
#    print ("Different")}
#} else {
# print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check(c(1,2),c(2,1)
elemnt_check(c(1,2),c(2,1))
elemnt_check(c(1,2),c(2,1))
elemnt_check(c(1,2),c(1,2))
elemnt_check(c(2,1),c(1,2))
elemnt_check <- function(x,y) {
# if (is.vector(x) & is.vector(y))
#  {
#     if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{
print("Exactly the same")
} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")} }
#     } else {
#    print ("Different")}
#} else {
# print("Error: x and y must be vectors")}
}
elemnt_check(c(1,2,3),c(3,2,1))
elemnt_check(c(1,2,3),c(1,2,3))
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{
print("Exactly the same")
} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")} }
} else {
print ("Different")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{
print("Exactly the same")
} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")} }
} else {
print ("Different")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(1,2)
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check(1,2)
elemnt_check(1,2)
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{print("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")}
print("Different")
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{print("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")}
print("Different")
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{print("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")}
print("Different")
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check(c(1,2,3),c(1,2,3))
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{print("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")}
print("Different")
} else {
print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{print("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")}
print("Different")
} else {
print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check(c(1,2,3),c(1,2,3))
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
#      if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{print("Exactly the same")} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")} }
print("Different")
#        } else {
#      print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
#      if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{print("Exactly the same")} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")} }
print("Different")
#        } else {
#      print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check(2,1)
elemnt_check(c(1,2,3),c(3,2,1)
elemnt_check <- function(x,y) {
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
#      if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{print("Exactly the same")} else {
if (sum(abs(sort(x)-sort(y))==0))
{print("The same")} }
print("Different")
#        } else {
#      print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
#      if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{return("Exactly the same")} else {
if (sum(abs(sort(x)-sort(y))==0))
{return("The same")} }
return("Different")
#        } else {
#      print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check(c(1,2,3),c(2,3,1))
elemnt_check(c(1,2,3),c(1,2,3))
elemnt_check(c(1,2,3),c(1,5,3))
elemnt_check(c(1,2,3),c(1,5,3))
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
#      if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{return("Exactly the same")} else {
if (sum(abs(sort(x)-sort(y))==0))
{return("The same")} }
return("Different")
#        } else {
#      print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(c(1,2,3),c(3,1,2)
elemnt_check(c(1,2,3),c(3,1,2))
elemnt_check(c(1,2,3),c(1,2,3))
elemnt_check <- function(x,y) {
#   if (is.vector(x) & is.vector(y))
#   {
#      if (length(x)==length(y))
#      {
if (sum(abs(x-y))==0)
{return("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{return("The same")}
return("Different")
#        } else {
#      print ("Different lengths")}
# } else {
# print("Error: x and y must be vectors")}
}
elemnt_check(1,2)
elemnt_check(1,1)
elemnt_check(c(1,2,3),c(3,2,1))
elemnt_check(c(1,2,3),c(1,2,3))
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{return("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{return("The same")}
return("Different")
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check(c(1,2,3),c(3,2,1))
elemnt_check(c(1,2,3),c(1,2,3))
elemnt_check(c(1,2,3),c(1,5,3))
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{return("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{return("The same")}
return("Different")
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{return("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{return("The same")}
return("Different")
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check(c(1,2,3),c(3,4,5))
elemnt_check(c(1,2,3),c(1,5,3))
sort(c(1,2,3)
sort(c(1,2,3))
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{return("Exactly the same")}
if (sum(abs(sort(x)-sort(y))==0))
{return("The same")}
return("Different")
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check <- function(x,y) {
if (is.vector(x) & is.vector(y))
{
if (length(x)==length(y))
{
if (sum(abs(x-y))==0)
{return("Exactly the same")}
else if (sum(abs(sort(x)-sort(y))==0))
{return("The same")}
else {("Different")}
} else {
print ("Different lengths")}
} else {
print("Error: x and y must be vectors")}
}
elemnt_check(c(1,2,3), c(1,2,3))
elemnt_check(c(1,2,3), c(1,2,5))
elemnt_check(c(1,2,3), c(1,2,3))
elemnt_check(c(1,2,3), c(1,2,3))
elemnt_check(c(1,2,3), c(1,2,5))
vec1 <- c(1,2,3)
vec2 <- c(3,2,1)
vec3 <- c(5,3,1)
vec1-vec2
abs(vec1-vec2)
sum(abs(vec1-vec2))
sum(abs(vec1-vec3))
sum(abs(vec1-vec1))
vec1-vec2
m <- matrix(nrow=12, ncol=12)
m <- matrix(nrow=12, ncol=12)
m <- matrix(nrow=12,ncol=12)
m <- matrix(nrow=12,ncol=12)
m <- matrix(nrow=12,ncol=12)
m <- matrix(nrow=12,ncol=12)
